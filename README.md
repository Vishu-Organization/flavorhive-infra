# FlavorHive Infrastructure

This repository contains Terraform code for deploying the Angular app (FlavorHive) to AWS using:
- S3 (static hosting)
- CloudFront (CDN)
- IAM (policies)

## Structure
- `main.tf` – main resources
- `variables.tf` – input variables
- `outputs.tf` – outputs
